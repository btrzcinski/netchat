# vim: ts=2 sw=2 sts=2 noexpandtab smartindent
config = {
	commands = {
		plugins = ("base", "modularity", "screen")
	}
	sets = {
		valid_sets = ("chat", "filetransfer", "mail")
		default_sets = ("chat", "filetransfer")
	}
	components = {
		default_components = ("commands",, "data")
	}
	modules = {
		default_modules = (,)
	}
}
